fhand = open('text-file-matrix.txt', 'r')
print(fhand)

# _io.TextIOWrapper represents a buffered text stream
# name='text-file-matrix.txt' is name of the file that is being opened ('text-file-matrix.txt')
# mode='r' is the mode in which the file is being opened (read-only mode)
# encoding='cp1252' specifies the encoding algorithm used to encode the contents of the file (CP-1252's legacy single-byte character encoding)
